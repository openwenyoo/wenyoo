# Dev-only test dependencies. Install with:  pip install -r requirements-test.txt
# Pinned minimally — see docs/contributing/test-harness-rollout.md §5.2.
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
pytest-cov>=5.0
# httpx / starlette.testclient are already transitive via FastAPI — no pin needed.
